export interface IProfile {
  name: string;

  title: string;
  headline: string;
  icon: string;
  colorCompose: string;
  resume: string;

  rol: string;
  features: string;
  softSkills: string;
  hardSkills: string;
  projects: string;
  blog: string;
}
